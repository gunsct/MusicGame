using UnityEngine;
using System.Collections;

public class GameUI : MonoBehaviour {
	//public GameObject[] button = new GameObject[6];
	// Use this for initialization
	void Start () {
		/*button [0].transform.position = new Vector3 (Screen.width *0.001f, Screen.height *0.001f, 0);
		button [1].transform.position = new Vector3 (Screen.width / 50.0f, Screen.height / 50.0f, 0);
		button [2].transform.position = new Vector3 (Screen.width / 50.0f, Screen.height / 50.0f, 0);
		button [3].transform.position = new Vector3 (Screen.width / 50.0f, Screen.height / 50.0f, 0);
		button [4].transform.position = new Vector3 (Screen.width / 50.0f, Screen.height / 50.0f, 0);
		button [5].transform.position = new Vector3 (Screen.width / 50.0f, Screen.height / 50.0f, 0);*/
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
